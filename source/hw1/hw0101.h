/**
 * @brief An interactive command line program that decodes reversed ASCII code.
 * This program reads a hex string from the standard input and prints the decoded string to the standard output.
 */
#include "helper.h"

 /**
  * @brief Decode (reverse) the input number.
  *
  * @param code A positive integer from 1 to 127.
  * @return The decoded character's ASCII code.
  */
u8 decode(u8 code);
