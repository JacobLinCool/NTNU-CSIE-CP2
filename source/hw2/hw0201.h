/**
 * @brief implement `mymatch()`
 * If any argument is `NULL` or the length of `pPattern` is 0, returns -1
 * Otherwise, returns the number of matches.
 * **NOTICE: Empty string will not be counted as a match**
 */
#include "mymatch.h"
